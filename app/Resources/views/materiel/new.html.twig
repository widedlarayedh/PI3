{% extends 'base.html.twig' %}

{% block content %}
    <h1>Materiel creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
      <input type="file" name="photoM" id="photoM">
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('materiel_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
